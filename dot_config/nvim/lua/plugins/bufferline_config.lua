return {
	"akinsho/bufferline.nvim",
	opts = {
		options = {
			separator_style = "slant",
		},
	},

	-- opts = function(_, opts) {
	--     opts.options.offsets = vim.tbl_extend("force", opts.options.offsets, {
	--         {
	--             filetype = "NvimTree",
	--             text = "File Explorer",
	--             highlight = "Directory",
	--             separator = true -- use a "true" to enable the default, or set your own character
	--         },
	--     })
	-- }
}
